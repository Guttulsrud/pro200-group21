import styled from 'styled-components';
import {
    background,
    border,
    color,
    flexbox,
    layout,
    margin,
    overflow,
    padding,
    position,
    shadow,
    space,
    typography
} from 'styled-system';


export const Div = styled.div`
 ${space}
 ${color}
 ${border}
 ${layout}
 ${flexbox}
 ${typography}
 ${position}
 ${margin}
 ${padding}
 ${shadow}
 ${overflow}
 ${background}

`;
